<UserControl x:Class="Finances.WinClient.Views.BankView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Finances.WinClient.Views"
             mc:Ignorable="d"
             d:DataContext="{d:DesignData Source=/DesignTimeData/Bank.xaml}">
    <Grid>
        <Border BorderThickness="1" BorderBrush="DarkCyan" CornerRadius="3" Padding="1">
            <StackPanel Orientation="Horizontal">
                <Border Height="20" Width="50" BorderThickness="1" BorderBrush="Thistle">
                    <Image Source="{Binding Logo}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="{Binding Name}" Padding="5,0,5,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
